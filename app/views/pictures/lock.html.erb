<h1>Lock</h1>

<%= form_for @picture, url: user_house_lock_path, html: {multipart: true} do |f| %>
  <p>
    <%= f.file_field :image, type: :file, multiple: true %>
  </p>

  <%= f.submit 'Lock' %>
<% end %>

<%= link_to 'back to profile', controller: 'users', action: 'dashboard' %>
