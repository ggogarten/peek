<h1>Unlock</h1>

<%= form_for @picture, url: user_house_unlock_path, html: {multipart: true} do |f| %>
  <p>
    <%= f.file_field :image, type: :file, multiple: true %>
  </p>

  <%= f.submit 'Unlock' %>
<% end %>

<%= link_to 'back to profile', controller: 'users', action: 'dashboard' %>
